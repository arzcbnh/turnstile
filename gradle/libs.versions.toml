[versions]
fabric-api = "0.153.0+26.2"
fabric-loader = "0.19.3"
fabric-loom = "1.17-SNAPSHOT"
minecraft = "26.2"
serverTranslations = "3.1.0+26.2"
spotless = "8.8.0"

[libraries]
fabric-api = { module = "net.fabricmc.fabric-api:fabric-api", version.ref = "fabric-api" }
fabric-loader = { module = "net.fabricmc:fabric-loader", version.ref = "fabric-loader" }
minecraft = { module = "com.mojang:minecraft", version.ref = "minecraft" }
serverTranslations = { module = "github-release:Server-Translations", version.ref = "serverTranslations"}
serverTranslations-api = { module = "xyz.nucleoid:server-translations-api", version.ref = "serverTranslations" }

[plugins]
fabric-loom = { id = "net.fabricmc.fabric-loom", version.ref = "fabric-loom" }
spotless = { id = "com.diffplug.spotless", version.ref = "spotless" }
